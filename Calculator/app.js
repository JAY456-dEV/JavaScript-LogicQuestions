const btn = document.querySelectorAll('.btn')
const displayValue = document.querySelector('.display')

let string = ''

btn.forEach((nums, idx) => {
    nums.addEventListener('click', function (e) {

        if (isNaN()) {
            
        }

    })
})


